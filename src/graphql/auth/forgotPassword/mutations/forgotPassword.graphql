mutation ForgotPassword(
  $email: String!
  $url: String!
  $instances: ID!
  $lang: String!
) {
  forgotPassword(
    input: {
      email: $email
      url: $url
      instanceId: $instances
      lang: $lang
    }
  ) {
    emailSent
  }
}