query companyTypesQuery($instanceIds: [ID]!) {
  companyTypes(instances: $instanceIds) {
    edges {
      node {
        id
        name
        __typename
      }
      __typename
    }
    __typename
  }
}

